const { createApp } = Vue;

createApp({
    data() {
        return {
            emails: [],  // Array per memorizzare gli indirizzi email generati
        }
    },
    methods: {
        
    }
}).mount('#app');
